import os
import asyncio
import threading
import sqlite3

from flask import Flask
from pyrogram import Client, filters
from pyrogram.types import ReplyKeyboardMarkup, ReplyKeyboardRemove
from openai import OpenAI


# ============================================================
# НАСТРОЙКИ
# ============================================================

API_ID = int(os.environ.get("API_ID"))
API_HASH = os.environ.get("API_HASH")
TELEGRAM_TOKEN = os.environ.get("TELEGRAM_TOKEN")
OPENAI_API_KEY = os.environ.get("OPENAI_API_KEY")
MANAGER_CONTACT = os.environ.get("MANAGER_CONTACT")


# ============================================================
# ПРОВЕРКА НАСТРОЕК
# ============================================================

print("========================================")
print("🔍 ПРОВЕРКА НАСТРОЕК")
print("========================================")

print("API_ID найден:", bool(API_ID))
print("API_HASH найден:", bool(API_HASH))
print("TELEGRAM_TOKEN найден:", bool(TELEGRAM_TOKEN))
print("OPENAI_API_KEY найден:", bool(OPENAI_API_KEY))

print("========================================")


if not TELEGRAM_TOKEN:
    raise ValueError("❌ TELEGRAM_TOKEN не найден")

if not API_HASH:
    raise ValueError("❌ API_HASH не найден")

if not OPENAI_API_KEY:
    raise ValueError("❌ OPENAI_API_KEY не найден")


# ============================================================
# OPENAI
# ============================================================

openai_client = OpenAI(
    api_key=OPENAI_API_KEY
)


# ============================================================
# СИСТЕМНЫЙ ПРОМПТ
# ============================================================

SYSTEM_PROMPT = """
Ты — виртуальный помощник Ирины, турагента. Общайся с клиентами в Telegram и помогай подобрать направление и отель. Отвечай по-русски, дружелюбно, естественно и кратко, без канцелярита и лишних объяснений.

КОНТЕКСТ
Всегда учитывай историю диалога и не спрашивай повторно то, что клиент уже сообщил.
Учитывай:
- город вылета;
- направление;
- даты/месяц и количество ночей;
- взрослых и детей с возрастом;
- бюджет;
- пожелания к отдыху, отелю и питанию;
- уже обсуждавшиеся варианты.

Понимай контекстные фразы вроде «туда», «этот», «первый», «подешевле», «на неделю».

НАПРАВЛЕНИЯ
Работай только с:
Турция, Египет, ОАЭ, Катар, Оман, Тунис, Абхазия, Сочи, Таиланд, Вьетнам, Китай, Шри-Ланка, Гоа, Бали, Сейшелы, Мальдивы, Занзибар.

Если пользователь спрашивает о другом направлении:
«С этим направлением Ирина сейчас не работает. Но могу подобрать похожий вариант из тех, с которыми мы работаем.»

ПОДБОР
Постепенно выясняй недостающие данные:
город вылета → даты/месяц → ночи → состав туристов → бюджет → пожелания.

Не превращай диалог в анкету. Если информации достаточно — сразу переходи к рекомендациям.

Обычно предлагай 2–4 подходящих варианта и объясняй, почему они подходят. Если один явно лучше — рекомендуй его.

При выборе отеля учитывай:
пляж, море, питание, номера, территорию, инфраструктуру, развлечения, детей, расположение и соотношение цены/качества.

АКТУАЛЬНАЯ ИНФОРМАЦИЯ И WEB SEARCH

Если вопрос касается актуальной информации, ОБЯЗАТЕЛЬНО используй web search, если tool доступен.

Особенно обязательно искать в интернете, если пользователь:
- спрашивает цену;
- просит найти тур;
- просит подобрать отель на конкретные даты;
- спрашивает наличие номеров;
- спрашивает актуальные условия бронирования;
- просит сравнить актуальные цены;
- спрашивает расписание рейсов;
- спрашивает свежие отзывы;
- спрашивает актуальные правила въезда.

ВАЖНО:
Если пользователь просит актуальную цену или наличие, НЕ отправляй его сразу к менеджеру. Сначала попытайся найти актуальную информацию через web search.

При поиске:
1. Учитывай все известные параметры из диалога.
2. Ищи свежую информацию.
3. Предпочитай официальные сайты отелей, авиакомпаний, туроператоров и другие надежные источники.
4. Если найденные данные относятся к другой дате, составу туристов или условиям — не выдавай их за подходящие пользователю.
5. Если точных данных найти не удалось — честно скажи об этом.

Никогда не выдумывай цену, наличие, скидки, акции, рейсы или условия бронирования.

Если актуальная цена найдена через web search, сообщи:
- название отеля/тура;
- цену;
- даты;
- состав туристов, если известен;
- что именно входит в стоимость;
- источник/сайт, если это уместно.

Цена из поиска — это найденная на момент проверки информация, а не гарантия бронирования.

ОТЕЛИ
Если пользователь спрашивает об отеле, используй web search для актуальной информации, если вопрос относится к текущим ценам, наличию, отзывам или условиям.

Не придумывай отзывы и характеристики.

Если сравниваешь отели — дай конкретный вывод:
«Если важнее пляж — я бы выбрал первый. Если важнее территория и развлечения для детей — второй.»

ПЕРЕДАЧА МЕНЕДЖЕРУ

Передавай клиента менеджеру, если он:
- хочет забронировать;
- готов покупать;
- хочет оформить договор или оплату;
- просит связать с человеком;
- либо актуальную информацию не удалось надежно найти через web search.

Если пользователь просто спрашивает цену — сначала используй web search.

При передаче менеджеру используй контакт:
{MANAGER_CONTACT}

Не отправляй клиента к менеджеру без необходимости.

СОМНЕНИЯ
Если дорого — предложи изменить даты, количество ночей, категорию отеля или направление.
Если сомневается между вариантами — сравни и порекомендуй.
Не дави и не создавай искусственную срочность.

ПРАВИЛА ВЪЕЗДА
Не выдумывай визовые и миграционные правила. Для актуальных правил используй web search.

ПЕРСОНАЛЬНЫЕ ДАННЫЕ
Не проси присылать паспорта, номера карт и другие чувствительные данные. Для оформления направляй к менеджеру.

ГЛАВНЫЕ ПРАВИЛА
Никогда не:
- выдумывай факты, цены или наличие;
- выдавай старую информацию за актуальную;
- обещай бронирование;
- утверждай, что тур забронирован;
- повторно спрашивай уже известные данные;
- отправляй клиента к менеджеру, если можешь помочь самостоятельно.

ФОРМАТ
Обычно 1–3 коротких абзаца. Не перегружай ответ таблицами и длинными списками. Если нужен уточняющий вопрос — задай только самый важный.

Главная цель:
понять клиента → собрать недостающие данные → найти актуальную информацию → помочь выбрать → при необходимости передать менеджеру.
"""


# ============================================================
# DATABASE
# ============================================================

DATABASE_PATH = "bot_memory.db"


def init_database():

    connection = sqlite3.connect(DATABASE_PATH)

    cursor = connection.cursor()

    cursor.execute("""
        CREATE TABLE IF NOT EXISTS messages (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            user_id INTEGER NOT NULL,
            role TEXT NOT NULL,
            content TEXT NOT NULL,
            created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
        )
    """)

    connection.commit()
    connection.close()

    print("🗄️ База данных готова", flush=True)


def save_message(user_id, role, content):

    connection = sqlite3.connect(DATABASE_PATH)

    cursor = connection.cursor()

    cursor.execute(
        """
        INSERT INTO messages
        (user_id, role, content)
        VALUES (?, ?, ?)
        """,
        (user_id, role, content)
    )

    connection.commit()
    connection.close()


def get_history(user_id, limit=20):

    connection = sqlite3.connect(DATABASE_PATH)

    cursor = connection.cursor()

    cursor.execute(
        """
        SELECT role, content
        FROM messages
        WHERE user_id = ?
        ORDER BY id DESC
        LIMIT ?
        """,
        (user_id, limit)
    )

    rows = cursor.fetchall()

    connection.close()

    # Мы получили сообщения от новых к старым.
    # Для OpenAI нужно наоборот.
    rows.reverse()

    history = []

    for role, content in rows:

        history.append({
            "role": role,
            "content": content
        })

    return history


def clear_history(user_id):

    connection = sqlite3.connect(DATABASE_PATH)

    cursor = connection.cursor()

    cursor.execute(
        """
        DELETE FROM messages
        WHERE user_id = ?
        """,
        (user_id,)
    )

    connection.commit()
    connection.close()

    print(
        f"🧹 История пользователя {user_id} очищена",
        flush=True
    )


# ============================================================
# FLASK
# ============================================================

flask_app = Flask(__name__)


@flask_app.route("/")
def home():

    return "Бот работает!", 200


@flask_app.route("/health")
def health():

    return "OK", 200


def run_flask():

    port = int(os.environ.get("PORT", 10000))

    print(
        f"🌐 Запускаю Flask на порту {port}",
        flush=True
    )

    flask_app.run(
        host="0.0.0.0",
        port=port
    )


# ============================================================
# TELEGRAM
# ============================================================

app = Client(
    "my_bot",
    api_id=API_ID,
    api_hash=API_HASH,
    bot_token=TELEGRAM_TOKEN
)


# ============================================================
# OPENAI
# ============================================================

def ask_gpt(user_id, user_message):

    print("========================================")
    print("🤖 НАЧАЛО OPENAI ЗАПРОСА")
    print("========================================")

    print(
        "👤 Пользователь:",
        user_id,
        flush=True
    )

    print(
        "💬 Сообщение:",
        user_message,
        flush=True
    )

    # --------------------------------------------------------
    # Получаем предыдущую историю
    # --------------------------------------------------------

    history = get_history(
        user_id,
        limit=20
    )

    print(
        f"🧠 Получено сообщений из памяти: {len(history)}",
        flush=True
    )

    # --------------------------------------------------------
    # Добавляем новое сообщение пользователя
    # --------------------------------------------------------

    messages = history + [
        {
            "role": "user",
            "content": user_message
        }
    ]

    # --------------------------------------------------------
    # Запрос в OpenAI
    # --------------------------------------------------------

    print(
        "📡 Вызываю OpenAI...",
        flush=True
    )

    try:

        response = openai_client.responses.create(

            # ------------------------------------------------
            # Модель
            # ------------------------------------------------

            model="gpt-4.1-mini",

            # ------------------------------------------------
            # Системная инструкция
            # ------------------------------------------------

            input=[
                {
                    "role": "developer",
                    "content": SYSTEM_PROMPT
                }
            ] + messages,

            # ------------------------------------------------
            # TOOLS
            # ------------------------------------------------

            tools=[
                {
                    "type": "web_search"
                }
            ]
        )

        # ----------------------------------------------------
        # Получаем текст ответа
        # ----------------------------------------------------

        answer = response.output_text

        print("========================================")
        print("✅ OPENAI ОТВЕТИЛ")
        print("========================================")

        print(
            answer,
            flush=True
        )

        # ----------------------------------------------------
        # Сохраняем сообщение пользователя
        # ----------------------------------------------------

        save_message(
            user_id,
            "user",
            user_message
        )

        # ----------------------------------------------------
        # Сохраняем ответ AI
        # ----------------------------------------------------

        save_message(
            user_id,
            "assistant",
            answer
        )

        print(
            "💾 Диалог сохранён в базу",
            flush=True
        )

        return answer

    except Exception as e:

        print("========================================")
        print("❌ ОШИБКА OPENAI")
        print("========================================")

        print(
            "Тип:",
            type(e).__name__,
            flush=True
        )

        print(
            "Ошибка:",
            str(e),
            flush=True
        )

        print("========================================")

        raise

# ============================================================
# /START
# ============================================================

@app.on_message(filters.command("start"))
async def start(client, message):

    user_id = message.from_user.id

    print(
        f"🚀 Пользователь {user_id} нажал /start",
        flush=True
    )

    await message.reply(
        "👋 Привет!\n\n"
        "Я помощник по путешествиям офиса Anex на Звездном бульваре. Я использую много личной информации, которой меня обучила сама Ирина.\n\n"
        "Можешь просто написать:\n\n"
        "🇮🇹 Хочу в Италию\n"
        "✈️ Куда поехать в сентябре?\n"
        "💰 Куда съездить недорого?\n\n"
    )


# ============================================================
# /CLEAR
# ============================================================

@app.on_message(filters.command("clear"))
async def clear(client, message):

    user_id = message.from_user.id

    clear_history(user_id)

    await message.reply(
        "🧹 Память очищена.\n\n"
        "Начинаем разговор с чистого листа."
    )


# ============================================================
# СООБЩЕНИЯ ПОЛЬЗОВАТЕЛЯ
# ============================================================

@app.on_message(filters.text)
async def chat_with_gpt(client, message):

    # Не обрабатываем команды
    if message.text.startswith("/"):
        return

    user_id = message.from_user.id

    print("====================================")
    print(
        "📩 ПОЛУЧЕНО СООБЩЕНИЕ:",
        message.text,
        flush=True
    )
    print(
        "👤 USER ID:",
        user_id,
        flush=True
    )
    print("====================================")

    try:

        print(
            "➡️ Передаю сообщение в ask_gpt()",
            flush=True
        )

        answer = await asyncio.to_thread(
            ask_gpt,
            user_id,
            message.text
        )

        print(
            "⬅️ ask_gpt() вернул ответ",
            flush=True
        )

        await message.reply(answer)

        print(
            "📤 Ответ отправлен пользователю",
            flush=True
        )

    except Exception as e:

        print("====================================")
        print("💥 ОШИБКА ОБРАБОТЧИКА")
        print("====================================")

        print(
            "Класс:",
            type(e).__name__,
            flush=True
        )

        print(
            "Ошибка:",
            str(e),
            flush=True
        )

        print("====================================")

        await message.reply(
            "😔 Произошла ошибка при обращении к AI.\n\n"
            "Попробуй ещё раз."
        )


# ============================================================
# ЗАПУСК
# ============================================================

if __name__ == "__main__":

    print("========================================")
    print("🚀 ЗАПУСК TELEGRAM-БОТА")
    print("========================================")

    # Создаём базу данных
    init_database()

    # Запускаем Flask
    flask_thread = threading.Thread(
        target=run_flask,
        daemon=True
    )

    flask_thread.start()

    print(
        "🌐 Flask запущен",
        flush=True
    )

    print(
        "🤖 Запускаю Telegram...",
        flush=True
    )

    app.run()

    print(
        "🛑 Telegram-бот остановлен",
        flush=True
    )
